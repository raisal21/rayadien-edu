---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ProgressBar from '../components/ProgressBar.astro';
import TopBar from '../components/TopBar.astro';
import Hero from '../components/Hero.astro';
import EntryCard from '../components/EntryCard.astro';
import BagianHeader from '../components/BagianHeader.astro';
import TopicDivider from '../components/TopicDivider.astro';
import Footer from '../components/Footer.astro';
import BackToTop from '../components/BackToTop.astro';
import Lightbox from '../components/Lightbox.astro';

const entries = (await getCollection('entries')).filter(e => e.data.subject === 'ekonomi');
const byBagian = (b: string) =>
  entries.filter(e => e.data.bagian === b).sort((a, c) => a.data.nomor - c.data.nomor);

const aEntries = byBagian('A');
const aGroups: { topic: string; items: typeof aEntries }[] = [];
for (const e of aEntries) {
  let g = aGroups.find(x => x.topic === e.data.topic);
  if (!g) { g = { topic: e.data.topic, items: [] }; aGroups.push(g); }
  g.items.push(e);
}

const bEntries = byBagian('B');
const cEntries = byBagian('C');

const TOPIC_LABEL: Record<string, string> = {
  mikro: 'Topik 1 · Mikroekonomi',
  pembayaran: 'Topik 2 · Sistem Pembayaran & Uang',
  lembaga: 'Topik 3 · Lembaga Keuangan',
};
const rangeOf = (items: typeof aEntries) =>
  `No. ${items[0]?.data.nomor}–${items[items.length - 1]?.data.nomor}`;
---

<BaseLayout title="Ekonomi Kelas X — Pasar, Uang & Lembaga Keuangan">
  <ProgressBar />
  <TopBar
    subject="ekonomi"
    brand="Ekonomi X"
    brandSub="Kisi-Kisi ASAT"
    filters={[
      { value: 'all', label: 'Semua' },
      { value: 'mikro', label: 'Mikro' },
      { value: 'pembayaran', label: 'Pembayaran' },
      { value: 'lembaga', label: 'Lembaga' },
    ]}
  />
  <Hero
    eyebrow="Materi Belajar &middot; ASAT Genap 2025/2026"
    titleMain="Ekonomi"
    titleEm="Kelas X"
    titleSub="Pasar, Uang & Lembaga Keuangan"
    lede="Catatan ringkas untuk asesmen akhir tahun &mdash; disusun mengikuti <b>kisi-kisi</b>: elastisitas, biaya produksi, struktur pasar, sistem pembayaran, serta OJK & lembaga keuangan."
    src="Sumber: rangkuman materi ekonomi SMA &mdash; selaraskan dengan catatan & buku pegangan kelas."
    legend={[
      { cls: 'n', label: 'Mikroekonomi' },
      { cls: 'n', label: 'Sistem Pembayaran' },
      { cls: 'n', label: '35 soal &middot; A 25 &middot; B 5 &middot; C 5' },
    ]}
    flashHref={false}
    howto="<b>Cara pakai:</b> baca <em>indikator</em> &#x2192; pahami <em>inti materi</em> &#x2192; cermati <em>contoh hitungan</em> &#x2192; buka <em>trik jawab</em>. Pakai tombol topik di atas untuk fokus, atau ketik di kotak <em>cari</em>."
  />

  <main class="wrap reading" id="main">
    <BagianHeader bagian="A" title="Pilihan Ganda" count={aEntries.length} />
    {aGroups.map(g => (
      <>
        <TopicDivider topic={g.topic} label={TOPIC_LABEL[g.topic] ?? g.topic} range={rangeOf(g.items)} />
        {g.items.map(entry => <EntryCard entry={entry} />)}
      </>
    ))}

    <BagianHeader
      bagian="B"
      title="Pilihan Ganda Kompleks"
      count={bEntries.length}
      note="⚠️ PG Kompleks = <u>jawaban benar bisa lebih dari satu</u>. Centang <strong>semua</strong> yang tepat, jangan berhenti di satu."
    />
    {bEntries.map(entry => <EntryCard entry={entry} />)}

    <BagianHeader
      bagian="C"
      title="Isian Singkat"
      count={cEntries.length}
      note="✍️ Isian singkat = jawab <strong>ringkas & tepat</strong> (istilah/angka/hasil hitungan)."
    />
    {cEntries.map(entry => <EntryCard entry={entry} />)}

    <Footer />
  </main>

  <BackToTop />
  <Lightbox />

  <script>
    import '../scripts/interactive.ts';
  </script>
</BaseLayout>
