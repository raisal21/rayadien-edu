---
interface Plate {
  src: string;
  alt: string;
  cap: string;
  label: string;
  width?: number;
  height?: number;
}

interface Props {
  topic: string;
  title: string;
  subtitle: string;
  plates: Plate[];
}

const { topic, title, subtitle, plates } = Astro.props;
---

<section class="overview reveal" data-topic={topic} aria-label={`Peta materi ${title}`}>
  <p class="overview-h">Peta Materi &middot; {title}</p>
  <p class="overview-s">{subtitle}</p>
  <div class="plates2">
    {plates.map((p, i) => (
      <figure class="plate">
        <button class="plate-frame" data-cap={p.cap}>
          <img src={`/${p.src}`} loading="lazy" alt={p.alt} width={p.width} height={p.height}>
        </button>
        <figcaption>
          <span class="cap"><b>Gambar {i + 1}</b>{p.label}</span>
          <span class="zoom">&#x2295; perbesar</span>
        </figcaption>
      </figure>
    ))}
  </div>
</section>
